<!DOCTYPE html>
<html>
  <head>
    <title>NinjaNinja</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
    <link href='https://fonts.googleapis.com/css?family=Kaushan+Script' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="clearfix">
      <div class="header_inner">
        <%= link_to "NinjaNinja", root_path, class: "name" %>
        <nav>
          <% if user_signed_in? %>
            <%= link_to "All Jobs", root_path %>
            <%= link_to "New Job", new_job_path %>
            <%= link_to "Account", edit_user_registration_path %>
            <%= link_to "Check Out", destroy_user_session_path, method: :delete %>
          <% else %>
            <%= link_to "All Jobs", root_path %>
            <%= link_to "Become a Ninja", new_user_registration_path %>
            <%= link_to "Check In", new_user_session_path %>
          <% end %>
        </nav>
      </div>
    </header>

    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% elsif alert %>
      <p class="alert"><%= alert %></p>
    <% end %>
      
    <%= yield %>

  </body>
</html>
